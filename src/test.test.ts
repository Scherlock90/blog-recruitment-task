import '@testing-library/jest-dom/extend-expect';

describe('test value container src-containers-input-decimal-index.test.tsx => InputDecimalContainer', () => {
  it('sums numbers', () => {
    expect(3).toEqual(3);
    expect(4).toEqual(4);
  });
});
